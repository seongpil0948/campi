import 'package:flutter/material.dart';

class FeedPostPage extends StatelessWidget {
  const FeedPostPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Center(
        child: Text("Feed Post Page"),
      ),
    );
  }
}
